type Props = {};

function Footer({}: Props) {
  return (
    <footer
      style={{
        height: "5rem",
        backgroundColor: "#05204a",
      }}
    ></footer>
  );
}

export default Footer;
