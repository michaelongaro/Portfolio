import React from "react";

type Props = {
  title: string;
  icons: string[];
};

function ProjectTitleAndIcons(props: Props) {
  // object for "React" -> icons filepath

  // do this later

  return <div>ProjectTitleAndIcons</div>;
}

export default ProjectTitleAndIcons;
